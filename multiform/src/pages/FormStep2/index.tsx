import * as C from './styles';
import {Theme} from '../../components/Theme';

export const FormStep2=()=>{
  return(
    <Theme>
      <C.Container>
        Passo 2
      </C.Container>
    </Theme>
  );
}